pypdf>=5.0
pdfplumber>=0.11
scikit-learn>=1.3
numpy>=1.26
reportlab>=4.0
joblib>=1.3

# Web dashboard
flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0
google-genai>=0.2.0
Pillow>=10.0
pdf2image>=1.17

# Camelot for advanced table extraction (fallback only)
# Requires: brew install ghostscript
camelot-py[cv]>=0.11

# Optional: only needed if you want OCR fallback for scanned (image-only) pages
pytesseract>=0.3
# pdf2image and pytesseract also require the system binaries `poppler-utils`
# and `tesseract-ocr` to be installed (e.g. `apt install poppler-utils tesseract-ocr`)
